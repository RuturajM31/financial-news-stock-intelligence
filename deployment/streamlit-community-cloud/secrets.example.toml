# Example only. Never commit a real token.
# Use a read-only token for the private Full BERT repository only when the
# deployed Streamlit runtime cannot use a local final model artifact.
HF_TOKEN = "paste-read-only-token-in-streamlit-cloud-secrets"
