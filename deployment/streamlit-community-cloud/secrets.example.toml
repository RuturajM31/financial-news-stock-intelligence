# Example only. Do not copy real secrets into Git.
# Paste real values only into Streamlit Community Cloud's app secrets UI when needed.
# The current free public deployment package does not require a secret by default.

# Optional future API integration example:
# FNI_STREAMLIT_API_BASE_URL = "https://example-free-endpoint.invalid"
# FNI_API_KEY = "paste-only-in-streamlit-cloud-secrets-ui"
